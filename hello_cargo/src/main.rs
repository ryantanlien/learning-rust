//Create a new cargo project with `cargo new`
//Build with `cargo build` and build + run with `cargo run`
//Built files are by default placed in the `target` folder
//Compile without building an executable with `cargo check`
fn main() {
    println!("Hello, world!");
}
